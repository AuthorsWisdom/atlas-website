# XAtlas Session Log — 2026-03-26

**Project:** XAtlas — Real-time market intelligence platform
**Session duration:** ~18 hours (full build day)
**Nexus Protocol:** https://github.com/AuthorsWisdom/nexus-protocol
**Status at session end:** Live on Fly.io, mostly functional, several items pending

---

## Infrastructure Changes

### ⚠️ Major: Railway → Fly.io Migration
**What happened:** Railway had an active EU-West region incident that cascaded and killed all WebSocket streams. During troubleshooting, discovered the backend was deployed in Singapore — wrong region for Binance (HTTP 451 blocks all US IPs). Attempted to change region to Chicago (ord) which also blocked Binance. Finally migrated to Fly.io Frankfurt (fra).

**Time lost:** ~1 full day

**Final state:**
- Backend: Fly.io `fra` (Frankfurt) — `atlas-backend-silent-log-2366.fly.dev`
- Railway: **DELETED** — subscription cancelled
- Fly.io app name: `atlas-backend-silent-log-2366`
- Region: `fra` — LOCKED, never change to US region

**Key decisions locked:**
- Fly.io `fra` is permanent — Binance blocks all US IPs (HTTP 451)
- `auto_stop_machines = false` — never sleep
- `min_machines_running = 1` — always warm
- `memory = "1gb"` — 256MB too small for WebSocket streams
- CMD in Dockerfile must be explicit: `uvicorn main:app --host 0.0.0.0 --port 8080`

### Backend URL Changes
All Railway URLs replaced in `atlas-website` with Fly.io URL:
- Old: `https://web-production-e9e4b.up.railway.app`
- New: `https://atlas-backend-silent-log-2366.fly.dev`
- Env var renamed: `RAILWAY_URL` → `FLY_URL` in Vercel
- 18 files updated in atlas-website

---

## Features Built Today

### ✅ Financial News Aggregator
- 5 sources: Benzinga (via Polygon/Massive), Finnhub, SEC EDGAR RSS, Federal Register API, Alpaca News, Federal Reserve RSS
- Backend: `routers/news.py` — 5 endpoints: `/news/market`, `/news/sec`, `/news/regulations`, `/news/alpaca`, `/news/benzinga`, `/news/all`
- Frontend: News tab in dashboard with filter pills (All/Markets/SEC/Regulations/Fed)
- Homepage news section (3-column grid, 6 articles)
- Ticker-specific news in detail panel
- "View all news" links to `/app?tab=news`

### ✅ Vercel AI Gateway Integration
- `app/api/ai/chat/route.ts` — routes through Vercel AI Gateway
- BYOK users delegated to Fly.io backend (keys never leave server)
- Fallback to Fly.io if gateway unavailable
- `AI_GATEWAY_API_KEY` in Vercel env vars

### ✅ Conviction Score Architecture Fixed
- Root cause: `get_tier(None)` always returned "free" when `X-Atlas-Tier` absent
- Fix: Check `X-Is-Pro` header as fallback when `X-Atlas-Tier` missing
- Files: `main.py` lines 408-410 and 520-522
- Score cache TTL reduced: 15min → 60s
- Added `/admin/clear-cache` endpoint (GET)

### ✅ Separate Score State (Critical Architecture Fix)
- Conviction scores were merged into `quotes` state
- SSE price updates every second were wiping conviction data
- Fix: Separate `scores` state, never touched by price updates
- 60s TTL cache with `scoreTimestamps` ref
- Force-clear TTL when ticker selected for immediate fresh fetch
- Retry logic: 2 retries, 2s delay, `AbortSignal.timeout(10s)`

### ✅ AI Model Tier Fix
- `DEFAULT_MODEL_FREE = "claude-haiku-4-5-20251001"` (was Sonnet for all)
- `DEFAULT_MODEL_PRO = "claude-sonnet-4-5-20250514"`
- Free users no longer burning expensive Sonnet budget
- Model selector moved from Settings to AI chat window
- Only shows models for providers user has keys for

### ✅ BYOK Rate Limit Bug Fixed
- BYOK users were being rate-limited despite having own keys
- Root cause: `check_and_increment_usage()` called BEFORE BYOK key lookup
- Fix: Resolve BYOK key first, then check rate limit
- Pro limits raised: 50/500 → 100/1000 (daily/monthly)
- Owner ID bypass added for development testing

### ✅ WebSocket Reconnect Hardening
- Alpaca: replaced recursive reconnect with `while True` + exponential backoff
- Binance: added `_binance_connected` flag for accurate health reporting
- Connection limit: 120s wait (not aggressive retry) on 429
- Suppressed Alpaca/websockets library logs to prevent Railway log flooding
- Finnhub fallback covers stocks while Alpaca recovers

### ✅ Nexus Protocol Created
- Repo: https://github.com/AuthorsWisdom/nexus-protocol
- 26 files, 20 directories
- Supabase memory hub: nexus-protocol Supabase project
- Seeded with XAtlas decisions, pitfalls, session summaries
- Both atlas-website and atlas-backend wired as child projects
- `scripts/end-session.sh`, `scripts/search-decisions.sh`, `scripts/new-project.sh`

### ✅ Dashboard Improvements
- Market hours widget: PRE-MARKET/MARKET OPEN/AFTER-HOURS/MARKET CLOSED with countdown
- CRYPTO 24/7 badge
- AI Assistant — draggable floating window, model selector, context pills
- News sidebar below scanner (not overlapping)
- "Dashboard" label (not "App")
- XAtlas logo clickable → homepage
- Error boundary wrapping entire dashboard
- Bloomberg-style top bar

### ✅ Macro Tab Expansion
- 15 FRED indicators (was 6)
- AI Macro Analysis panel (Pro only, on-demand)
- Economic Calendar (FRED releases)
- Fed & Regulatory news feed

### ✅ Watchlist Tab Expansion
- Rich ticker cards with conviction bar
- Immediate score fetch on ticker add
- Watchlist news feed (per-symbol)

### ✅ Chart Fixes
- TradingView watermark removed (`attributionLogo: false`)
- Chart scroll capture disabled on mobile (`handleScroll.vertTouchDrag: false`)
- Nuclear cleanup on symbol/timeframe switch
- Guard against empty data

### ✅ Search Bar Fix
- `WatchlistSearch` extracted into `memo()` component
- Stable `useCallback` for add handler
- Focus no longer lost on price updates

### ✅ ETF/Sector Ticker Search
- Removed `market=stocks` filter from Polygon search
- Added 20+ ETF fallbacks (XLE, FANG, SOXL, TQQQ, ARKK, etc.)
- Results include ETF, ETP types

### ✅ Mobile Responsive Layout
- Bottom tab bar (60px fixed, safe area inset)
- Table → card stack on mobile
- Detail panel full screen on mobile
- Stats bar horizontal scroll
- `windowWidth` state with resize observer
- `isMobile` / `isTablet` breakpoints

### ✅ Homepage Improvements
- Stats bar: 16 indicators, 5+ sources, 100ms latency, 24/7 crypto
- Updated feature cards with specific capability descriptions
- "Dashboard" button (was "Open App")

---

## Pending Items

### 🔴 Critical
- **Conviction scores still not populating consistently** — added debug logging, needs console output to diagnose
- **Account page** — patched but needs verification after deploy
- **Apple Developer Portal approval** — applied 2026-03-23, still pending — blocks all iOS work

### 🟡 High Priority
- **Polygon 403 on options chain/flow** — Options Starter plan may not include `/v3/snapshot/options/` — needs plan verification
- **SPY/QQQ in stats bar** — showing `—` — these aren't in watchlist, overview fetch may be failing
- **Alpaca stream** — shows `connected: false` after market close — verify it reconnects at market open
- **Score breakdown bars** — showing 0 for some tickers — tied to conviction score issue

### 🟢 Enhancement Backlog
- Macro tab: historical sparklines per indicator
- Watchlist: correlation matrix
- Price alerts / push notifications
- Signal accuracy tracking
- Brokerage import
- App Store screenshots (blocked on Apple approval)
- Switch Stripe from sandbox to live mode (verify)
- Post on X/Twitter, Reddit (r/options, r/wallstreetbets)
- UptimeRobot monitoring setup

---

## Architecture State (End of Session)

### Infrastructure
```
Frontend:  Vercel (xatlas.io) — auto-deploys from atlas-website main
Backend:   Fly.io fra (atlas-backend-silent-log-2366.fly.dev)
Database:  Supabase (njslvxmfxfcveygjiyoq.supabase.co)
Auth:      Supabase Auth
iOS Subs:  RevenueCat (configured, pending Apple approval)
Web Subs:  Stripe (sandbox price IDs active)
Domain:    xatlas.io via Namecheap → Vercel DNS
```

### Data Streams (health at session end)
```
Binance:  ✅ connected — 19 crypto symbols (Frankfurt bypasses HTTP 451)
Finnhub:  ✅ connected — 8 stock quotes cached
Alpaca:   ❌ false — expected (market closed, IEX only active during trading hours)
```

### Env Vars Required
**Vercel (atlas-website):**
```
NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_URL, SUPABASE_SERVICE_KEY
STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET
STRIPE_PRICE_MONTHLY, STRIPE_PRICE_ANNUAL, STRIPE_PRICE_LIFETIME
FLY_URL=https://atlas-backend-silent-log-2366.fly.dev
AI_GATEWAY_API_KEY
```

**Fly.io (atlas-backend):**
```
ANTHROPIC_API_KEY, POLYGON_API_KEY, FINNHUB_API_KEY, FRED_API_KEY
SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_KEY, SUPABASE_JWT_SECRET
APCA_API_KEY_ID, APCA_API_SECRET_KEY
ENCRYPTION_KEY, REVENUECAT_WEBHOOK_SECRET
```

### Supabase Tables
```
profiles      — id, email, is_pro, subscription_source, subscription_status,
                stripe_customer_id, revenuecat_user_id, preferred_ai_model,
                preferred_ai_provider, anthropic_key_encrypted, openai_key_encrypted
waitlist      — email, created_at, source, converted_to_user
ai_usage      — user_id, date, daily_calls, monthly_calls, month, last_call_at
```

**ai_usage column names:** `daily_calls` / `monthly_calls` (NOT daily_count/monthly_count)
**ai_usage unique key:** `(user_id, date)` — NOT just user_id

---

## Key Pitfalls Discovered Today

| # | Pitfall | Time Lost | Rule |
|---|---|---|---|
| 1 | Railway region incident → all streams die | 3hr | Always check status.railway.com first |
| 2 | Singapore → Chicago → Frankfurt region odyssey | 1hr | Binance blocks ALL US IPs — always use fra |
| 3 | Alpaca connection limit after region change | 30min | Suspend old service before testing new |
| 4 | Fly.io running `fastapi run` not uvicorn | 30min | Always explicit CMD in Dockerfile |
| 5 | Docker cache serving stale image | 20min | `fly deploy --no-cache` after Dockerfile changes |
| 6 | Fly app not connected to GitHub | 15min | Connect GitHub before first deploy |
| 7 | Fly deployed to Amsterdam not Chicago | 15min | Set primary_region in fly.toml explicitly |
| 8 | Conviction scores wiped by price updates | 30min | Separate state for scores vs quotes |
| 9 | AI model Sonnet for all users | 20min | Pre-build grep, resolve_model() single source |
| 10 | BYOK users still rate-limited | 20min | Resolve BYOK key BEFORE calling rate limiter |
| 11 | Wrong terminal directory | 30min | Always pwd + full file paths in prompts |
| 12 | SQL on wrong Supabase project | 10min | XAtlas = njslvxmfxfcveygjiyoq, verify URL |
| 13 | Wrong column names (daily_count vs daily_calls) | 10min | Always check schema before writing queries |
| 14 | Chart hijacks mobile scroll | 20min | handleScroll.vertTouchDrag: false |
| 15 | Search input loses focus on keypress | 20min | memo() + useCallback pattern |
| 16 | Railway URLs hardcoded after migration | 15min | grep -rn "railway.app" after any migration |

---

## Decisions Locked This Session

1. **Fly.io fra region** — permanent, never change to US region
2. **Railway deprecated** — never use for WebSocket-heavy apps
3. **RAILWAY_URL → FLY_URL** — env var renamed in Vercel
4. **Scores separate from quotes** — never merge computed data into price state
5. **AI model tiers** — Free=Haiku, Pro=Sonnet, BYOK=user choice
6. **BYOK rate limit order** — resolve key BEFORE checking rate limit
7. **Fly.io CMD** — always explicit uvicorn, never auto-detect
8. **Binance rule** — non-US region ONLY (fra/ams/sin)

---

## Next Session Start Checklist

```bash
# 1. Verify backend health
curl "https://atlas-backend-silent-log-2366.fly.dev/health" | python3 -m json.tool

# 2. Check Alpaca reconnected (market hours)
# Should show connected: true between 9:30am-4pm ET

# 3. Check conviction scores working
# Open xatlas.io/app → DevTools console → click NVDA
# Look for [fetchScore] logs

# 4. Read locked decisions
cat ~/projects/atlas-website/LOCKED.md
cat ~/projects/atlas-backend/LOCKED.md

# 5. Check Apple Developer approval
# developer.apple.com — approval = unlock iOS work
```

---

## Monthly Cost (current)
```
Fly.io:          ~$5-10/mo
Polygon/Massive: $29/mo (Options Starter)
iubenda:         $27.99/mo
Domain:          ~$1/mo
Anthropic API:   ~$10-30/mo
Total:           ~$73-98/mo
Break-even:      3-4 Pro subscribers at $29.99/mo
```

---

*Generated by Nexus Protocol end-session protocol*
*Session date: 2026-03-26*
*Next session: Pick up from conviction score debugging (check [fetchScore] console logs)*
